/**
 * Legacy decorative canvas shapes.
 * The 2026 system UI refresh uses a clean white/light-neutral application canvas,
 * so the shared decorative layer intentionally renders nothing.
 */
export function CornerBlobs() {
  return null;
}
